{
  "name": "taskbar-icon-splitter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:extension": "node scripts/build-extension.mjs",
    "build:store-extension": "node scripts/build-extension.mjs --store",
    "test:extension": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "tldts": "^7.0.17"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.326",
    "esbuild": "^0.25.6",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
